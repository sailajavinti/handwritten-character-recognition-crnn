# handwritten-character-recognition-crnn
Handwritten Character Recognition system using Deep Learning. Built with CNN models trained on MNIST (digits) and EMNIST (letters), with CRNN architecture for sequence recognition. Includes Streamlit web app for real-time image prediction and deployment-ready ML pipeline.
